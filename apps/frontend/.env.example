# Source of truth:
#   ../sofortbot-infra/.env
# Keep this file only as a fallback template for standalone frontend runs.
NODE_ENV=development
PORT=8931
NEXT_PUBLIC_API_BASE_URL=http://localhost:8932/api/v1
NEXT_PUBLIC_SERVICES_API_BASE_URL=http://localhost:8934
NEXT_PUBLIC_APP_ENV=stage
NEXT_PUBLIC_ANDROID_APK_URL=
NEXT_PUBLIC_ANDROID_APK_URL_STAGE=
NEXT_PUBLIC_ANDROID_APK_URL_PROD=

# E2E auth (local Playwright runs only)
# Use your real test account credentials; never commit real secrets.
E2E_LOGIN=
E2E_PASSWORD=
